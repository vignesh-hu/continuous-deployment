# Custom config for AWS BeanStalk platform

branch-defaults:
  master:
    environment: demo-env
  global: 
  application_name: demo
  default_platform: Java 8 running on 64bit Amazon Linux/2.11.1
  default_region: us-east-1
  sc: git
deploy:
  artifact: target/demo-1.0.jar
